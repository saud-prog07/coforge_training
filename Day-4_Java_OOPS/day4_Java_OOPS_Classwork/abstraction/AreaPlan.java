package com.coforge.day4.abstraction;

public interface AreaPlan {
	public void area(); /*
						 * We need not write abstract here because all methods in 
						 * interface is abstract
						 */
}
